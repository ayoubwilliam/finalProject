# Core PyTorch ecosystem (CUDA recommended)
torch>=2.0.0
torchvision>=0.15.0

# Scientific computing and image processing
numpy>=1.24.0
scipy>=1.10.0
kornia>=0.7.0
Pillow>=10.0.0

# Medical Imaging (NIfTI & CT processing)
nibabel>=5.1.0
TotalSegmentator>=2.0.0
gryds==0.0.9

# Plotting and Metrics
matplotlib>=3.7.0
scikit-learn>=1.3.0
tqdm>=4.65.0

# GPU Acceleration for Distance Transforms
# Note: CuPy should be installed dynamically based on your CUDA version using the installation command provided in the README.
# cupy-cuda11x>=13.0.0
# cupy-cuda12x>=13.0.0
